<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= @user.errors.count %> Fehler!</h2>
    <p>Bitte füllen Sie folgende Felder mit validen Daten:</p>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

